<h1>Goals</h1>
<ul>
  <% @user.goals.each do |goal| %>
    <%if goal.ppublic == true || @user == current_user%>
      <li><%= link_to goal.content, goal_url(goal)%></li>
    <%end%>
  <%end%>
</ul>


<%=link_to "All Users", users_url%><br>
<%=link_to "Create New Goal", new_goal_url%>